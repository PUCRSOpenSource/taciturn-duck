<!DOCTYPE html>
<html>
	<head>
		<title>Sam</title>
		<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
		<%= csrf_meta_tags %>
	</head>
	<body>
		<%= render 'layouts/menu'%>
		<%= yield %>
		<footer>
			<div class="container">
				<p class="text-muted">Sistema Aconselhador de Matrícula</p>
			</div>
		</footer>
	</body>
</html>
